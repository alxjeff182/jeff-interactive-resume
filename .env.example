# Canonical URL for OG / Twitter / sitemap / JSON-LD. On Vercel, VERCEL_URL / VERCEL_PROJECT_PRODUCTION_URL are used automatically during build if unset.
# Set explicitly when using a custom domain so shared links match what users visit:
# VITE_SITE_URL=https://your-custom-domain.com
